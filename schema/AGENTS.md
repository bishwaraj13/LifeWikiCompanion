# Schema Instructions

The `schema/` directory is the constitution for the LifeWiki.

Use these files to decide:

- which frontmatter fields a page needs
- which Markdown headings a page should expose
- which prompt shape an LLM maintainer should follow
- which lint rules protect wiki health

When a new kind of wiki page is introduced, add its template here before broad implementation.
